# Registro de Cambios - Octopus Coquinaria
Fecha de creación: 2026-01-10

## 2026-01-10 - Visibilidad en Dashboard y Home
- **Dashboard**: Se agregó `TickerGastronomico` (tope) y `NewsBoard` (base).
- **Dashboard**: Se agregaron tarjetas de acceso rápido para "Calendario" y "Academia".
- **Layout**: Se ajustó el responsive del navbar para evitar superposiciones (Finanzas -> Fin, ocultar email).
- **Admin**: Se eliminó la redirección automática de Admins a `/admin/leads` para que puedan ver el Dashboard de usuario.
- **Home**: Se implementó contenido por defecto ("fallback") en `TickerGastronomico` y `NewsBoard` para asegurar que siempre sean visibles, incluso sin conexión a base de datos.
- **Home**: Se garantiza la visibilidad de accesos a Academia y Calendario para usuarios no logueados (Lead Catching).
- **Base de Datos**: Usuario ejecutó `FIX_PUBLIC_ACCESS_FINAL.sql`. Se habilitó lectura pública (RLS) para `recursos_academia`, `eventos_calendario` y `public_board_items`.
- **Hito**: Fin de problemas de "contenido oculto" para usuarios anónimos.
